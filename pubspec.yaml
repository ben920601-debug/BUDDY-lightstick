name: lightstick_controller
description: GFRIEND 手燈 BLE 顏色控制 MVP
publish_to: 'none'
version: 0.1.0+9

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_blue_plus: ^1.32.12
  permission_handler: ^11.3.1
  flutter_colorpicker: ^1.1.0
  noise_meter: ^4.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true